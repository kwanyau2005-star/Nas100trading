name: CI (manual only)

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Automated tests disabled
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Automated CI test run is disabled for this repository."
